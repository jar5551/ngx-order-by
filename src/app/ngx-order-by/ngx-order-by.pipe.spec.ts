import { NgxOrderByPipe } from './ngx-order-by.pipe';

describe('NgxOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
